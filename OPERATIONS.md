# 対応と売上の運用

ダッシュボードの「対応と売上」で、顧客別の対応・期限・作業時間・確認済み入金を記録できます。
既存の会員・鑑定書・LINEログは保持し、追加の `ops_events` シートに履歴を追記します。
Google Sheetsのレイアウトは既存と同じくB2が見出し、B3以降がデータです。

## 日々の使い方

1. 購入や相談が届いたら「対応を登録する」で必要な作業と期限を記録する。
2. 工程を「ヒアリング待ち→鑑定作成→納品待ち」などに進める。実際に済んだ時に完了へ変える。
3. 公式LINEで手動返信したら、会員相談画面の既存ボタン、または「対応と売上」で対応した受信範囲を記録する。
4. 作業後に所要時間を記録する。記録から運営者の負担を測り、返信・制作・事務のどこを改善するか決める。

手動対応は確認した受信の指紋（ID・日時・本文のハッシュ）に結びつけます。
新しい受信が後から来ても、一括して対応済みにはしません。手動返信のメモは会員相談の生成時にも渡します。
この記録は自動応答ボットを止める操作ではありません。

## Stripeの接続

受け口は既存のLINEサーバーの `/stripe/webhook` です。LINEのWebhook URLは変更しません。

Render側の環境変数:

- `STRIPE_WEBHOOK_SECRET`: この受け口用の署名シークレット。
- `STRIPE_WEBHOOK_LIVEMODE`: テストは `false`、本番は `true`。未指定時はテスト。

Stripeのイベント送信先で、次のsnapshotイベントを選びます。

```
charge.succeeded
charge.captured
refund.created
refund.updated
invoice.paid
invoice.payment_failed
customer.subscription.created
customer.subscription.updated
customer.subscription.deleted
```

まずテスト用の送信先と署名シークレットで受信を確認し、本番用に切り替えます。
署名シークレットやAPIキーはコミットやチャットに貼らず、Renderの環境変数へ直接入力してください。
この受け口はStripe APIキーを必要としません。過去の取引は自動では読み込みません。

署名を未加工のリクエスト本文で検証し、永続保存が成功してから200を返します。
イベントIDと決済・返金IDで重複計上を防ぎます。Sheetsでは同時書き込みや再試行によって
同じイベント行が複数になる可能性があるため、読み取り・集計側でもIDを重複排除します。

登録済みの入金・返金のみ集計します。Stripeテストイベントは本番集計から除外します。
JPY以外は別途表示し、換算しません。手数料・経費・過去の未登録取引を含む利益計算ではありません。
STORES・銀行振込の返金はこの版の集計対象外です。月次締めは決済サービスの明細と照合してください。
注文番号だけでは入金と扱いません。手動入金は管理画面で確認した実金額を入力します。

Stripe顧客IDとLINEユーザーIDは管理画面で照合して紐付けます。氏名・生年月日による自動照合はしません。
会員の登録・解約・サービス停止・支払い督促・顧客への通知は自動実行しません。
既存会員の契約額や利用期限はStripeと既存の案内内容を確認して判断してください。

## 鑑定書の要点ページ

個別鑑定書の表紙と目次の間に「今の整理・まずすること・今は控えること・見直す目安」を追加します。
完成した本文だけから要約し、各項目160字まで。本文にない日時や約束を補わない指示を設けています。
形式・生成完了を検査しますが、解釈の正しさまで機械で保証はできません。納品前に本文との一致を確認してください。
潮見に含まれる個別鑑定書にも適用されます。別ファイルの暦のレイアウトはこの版では変更しません。

## 学習ファイルの保護

`learn` と `voice-learn` は生成が正常に完了した場合のみ保存します。
空・短すぎる出力、重複表題、未来の日付を検査し、不正な場合は従来のファイルを保持します。
正常な出力は一時ファイルから原子的に置き換えます。既存の切れた学習文そのものを自動修復する機能ではありません。

## 検証と戻し方

```
pip install -r requirements-dev.txt
python -m pytest -q
```

テストは架空データとローカルSQLite・モックだけを使い、顧客への送信や本番決済を行いません。
デプロイ前のコミットへ戻すと従来画面へ戻ります。追加の `ops_events` は残しておいて構いません。
旧版はこのシートを参照しません。受け口を戻した場合は、Stripeの送信先を一時停止して再試行を確認してください。

参考: [Stripeの署名検証](https://docs.stripe.com/webhooks/signature)、
[Stripeイベント](https://docs.stripe.com/api/events/types)、
[Claudeの停止理由](https://platform.claude.com/docs/en/build-with-claude/handling-stop-reasons)。
